

<% unless @item.user_id == current_user.id %>

<%= button_to 'Hire', {:controller => "hires", :action => "create", :item_id => @item.id, :user_id=> current_user.id, :hired => 1} , :method=>:post  %>

<% end  %>

<% @item.hires.each do |hire| %>
    <% if hire.hired == true %>
    <h4> Item is unavailable</h4>
    <% end %>
<% end %>


<h1><%= @item.name %></h1>

<h3>This item belongs to <%=link_to @item.user.name, user_path(@item.user.id) %></h3>

<%= link_to 'edit me', edit_item_path(@item) %>


<%= link_to "All items", items_path %>
